// Each element in the tree has a priority associated with it.
// Elements with higher priorities are served before elements with lower priorities.
// Priority is defined from left to right.
// Use for Priority Queues is when we want to find the max or min value in a tree.